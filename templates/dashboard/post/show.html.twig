{% extends 'dashboard/base.html.twig' %}

{% block title %}Post{% endblock %}

{% block body %}
    <h1>Post</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ post.title }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ post.slug }}</td>
            </tr>
            <tr>
                <th>Body</th>
                <td>{{ post.body }}</td>
            </tr>
            <tr>
                <th>Category_id</th>
                <td>{{ post.categoryId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dashboard_post_index') }}">back to list</a>

    <a href="{{ path('dashboard_post_edit', {'id': post.id}) }}">edit</a>

    {{ include('dashboard/post/_delete_form.html.twig') }}
{% endblock %}
